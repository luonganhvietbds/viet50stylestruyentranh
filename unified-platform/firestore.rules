rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============================================
    // HELPER FUNCTIONS
    // ============================================
    
    // Check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }
    
    // List of admin emails (add your admin emails here)
    function isAdminEmail() {
      return request.auth.token.email in [
        'admin@example.com',
        'luonganhvietbds@gmail.com'
      ];
    }
    
    // Check if user is admin by role in Firestore
    function isAdminByRole() {
      return isAuthenticated() && 
        exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // Combined admin check - either by email or by role
    function isAdmin() {
      return isAuthenticated() && (isAdminEmail() || isAdminByRole());
    }
    
    // Check if user owns the document
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }
    
    // ============================================
    // USERS COLLECTION
    // ============================================
    match /users/{userId} {
      // Users can read their own profile
      allow read: if isOwner(userId);
      
      // Users can CREATE their own profile (for new registrations)
      allow create: if isOwner(userId);
      
      // Users can update their own profile (except role)
      allow update: if isOwner(userId) && 
        (!request.resource.data.diff(resource.data).affectedKeys().hasAny(['role']));
      
      // Admin can read all users
      allow read: if isAdmin();
      
      // Admin can create, update, delete any user
      allow create, update, delete: if isAdmin();
    }
    
    // ============================================
    // STYLES COLLECTION (Agent System)
    // ============================================
    match /styles/{styleId} {
      // ALL authenticated users can READ styles (for Story/Scene modules)
      allow read: if isAuthenticated();
      
      // Only admin can create, update, delete styles
      allow create, update, delete: if isAdmin();
    }
    
    // ============================================
    // STORIES COLLECTION (Story Factory)
    // ============================================
    match /stories/{storyId} {
      // Users can read their own stories, admin can read all
      allow read: if isAuthenticated() && 
        (resource.data.userId == request.auth.uid || isAdmin());
      
      // Users can create their own stories
      allow create: if isAuthenticated() && 
        request.resource.data.userId == request.auth.uid;
      
      // Users can update/delete their own stories
      allow update, delete: if isAuthenticated() && 
        (resource.data.userId == request.auth.uid || isAdmin());
    }
    
    // ============================================
    // SCENES COLLECTION (Scene Generator)
    // ============================================
    match /scenes/{sceneId} {
      // Users can read their own scenes
      allow read: if isAuthenticated() && 
        (resource.data.userId == request.auth.uid || isAdmin());
      
      // Users can create their own scenes
      allow create: if isAuthenticated() && 
        request.resource.data.userId == request.auth.uid;
      
      // Users can update/delete their own scenes
      allow update, delete: if isAuthenticated() && 
        (resource.data.userId == request.auth.uid || isAdmin());
    }
    
    // ============================================
    // JOBS COLLECTION (Pipeline Jobs)
    // ============================================
    match /jobs/{jobId} {
      // Users can read their own jobs
      allow read: if isAuthenticated() && 
        (resource.data.userId == request.auth.uid || isAdmin());
      
      // Users can create their own jobs
      allow create: if isAuthenticated() && 
        request.resource.data.userId == request.auth.uid;
      
      // Users can update/delete their own jobs
      allow update, delete: if isAuthenticated() && 
        (resource.data.userId == request.auth.uid || isAdmin());
    }
    
    // ============================================
    // TRANSACTIONS COLLECTION
    // ============================================
    match /transactions/{transactionId} {
      // Users can read their own transactions
      allow read: if isAuthenticated() && 
        (resource.data.userId == request.auth.uid || isAdmin());
      
      // Only admin can create/update/delete transactions
      allow create, update, delete: if isAdmin();
    }
    
    // ============================================
    // SYSTEM SETTINGS (Admin only)
    // ============================================
    match /settings/{settingId} {
      // Only admin can read/write settings
      allow read, write: if isAdmin();
    }
    
    // ============================================
    // LOGS COLLECTION
    // ============================================
    match /logs/{logId} {
      // Only admin can read logs
      allow read: if isAdmin();
      // Any authenticated user can create logs
      allow create: if isAuthenticated();
      // No one can update/delete logs
      allow update, delete: if false;
    }
    
    // ============================================
    // FALLBACK: Deny all other access
    // ============================================
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
